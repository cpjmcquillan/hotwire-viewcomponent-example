<% if @message.persisted? %>
  <%= turbo_stream.replace "new_message_form", partial: "form", locals: {new_message: new_message} %>
  <%= turbo_stream.append "messages" do %>
    <%= render MessageComponent.new(message: @message) %>
  <% end %>
<% end %>
